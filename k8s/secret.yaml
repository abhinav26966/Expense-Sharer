# =============================================================================
# Secret - Sensitive configuration (EXAMPLE ONLY)
# =============================================================================
# WARNING: In production, use sealed-secrets, external-secrets, or 
# cloud provider secret management (AWS Secrets Manager, Azure Key Vault, etc.)
# 
# This file is for demonstration purposes. NEVER commit real secrets to Git.
# The actual secret should be created via kubectl or CI/CD pipeline.
# =============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: expense-sharer-secret
  namespace: expense-sharer
  labels:
    app: expense-sharer
type: Opaque
data:
  # Base64 encoded value - in production, this would be a strong secret
  # echo -n "change-me-in-production" | base64
  SECRET_KEY: Y2hhbmdlLW1lLWluLXByb2R1Y3Rpb24=
